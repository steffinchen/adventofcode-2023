import { Day } from './day.type.js';

export class Day1 implements Day {
  input: string[] = [];

  constructor(input: string[]) {
    this.input = input;
  }

  part1 = () => {
    return 0;
  };

  part2 = () => {
    return 0;
  };
}
